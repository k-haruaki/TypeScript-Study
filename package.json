{
  "name": "typescript-deep-dive",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npx tsc --build ./tsconfig.json",
    "start": "node ./dist/$npm_config_filename",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/react": "^18.2.6",
    "typescript": "^4.9.3"
  }
}
